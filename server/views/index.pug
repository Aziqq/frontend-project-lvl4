doctype html
html.h-100(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Chat
    link(href=assetPath('main.css') rel="stylesheet")
    script.
      var gon = !{JSON.stringify(gon)};
  body.vh-100.d-flex.flex-column.h-100#chat

    script(src=assetPath('main.js'))
